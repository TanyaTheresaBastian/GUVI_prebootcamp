var a = 2 > 12; //Previously, values were strings, when changed to numbers, condition else works.
//Don't touch below this
if (a) {
  console.log("Code is Blasted") //if the no 2 is greater than 12 and when strings are used instead of nos.
}
else
{
  console.log("Diffused") // because the no 2 is not greater than 12, code is diffused.
}